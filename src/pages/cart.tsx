import React from "react"
import Layout from "../components/layout"

interface Props {}

const cart: React.FC<Props> = ({}) => {
  return (
    <Layout>
      <div></div>
    </Layout>
  )
}
export default cart
